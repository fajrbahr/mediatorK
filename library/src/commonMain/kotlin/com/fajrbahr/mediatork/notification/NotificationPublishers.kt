package com.fajrbahr.mediatork.notification

/** Top-level alias so callers can import via `notification.*` without qualifying the interface name. */
typealias SequentialNotificationPublisher = NotificationPublishStrategy.SequentialNotificationPublisher

/** Top-level alias so callers can import via `notification.*` without qualifying the interface name. */
typealias ParallelNotificationPublisher = NotificationPublishStrategy.ParallelNotificationPublisher

/** Top-level alias so callers can import via `notification.*` without qualifying the interface name. */
typealias ContinueOnExceptionNotificationPublisher = NotificationPublishStrategy.ContinueOnExceptionNotificationPublisher

/** Top-level alias so callers can import via `notification.*` without qualifying the interface name. */
typealias FireAndForgetNotificationPublisher = NotificationPublishStrategy.FireAndForgetNotificationPublisher
